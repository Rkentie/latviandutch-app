// Game configuration constants
export const ROUND_OPTIONS = [5, 10, 20, 50, 180] as const;
export const MARATHON_SIZE = 180;

// Default round size index (maps to 10 words)
export const DEFAULT_ROUND_SIZE_INDEX = 1;

